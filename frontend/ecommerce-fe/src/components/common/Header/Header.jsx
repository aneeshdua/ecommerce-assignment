import React from 'react';
import { Typography } from '@mui/material';

const Header = () => {
    return (
        <header>
            <Typography variant="h2">Ecommerce Platform</Typography>
        </header>
    );
};

export default Header;